Loading python/3.7.3
  Loading requirement: tcl/8.6.8 gcc/8.3.0
Constructing dataset
     Inconsistent observations in new_cases in JH CSSE dataset: 49
Splicing database into subsets
Getting Kalman filter estimates
Constructing STAN models
DIAGNOSTIC(S) FROM PARSER:
Info:
Left-hand side of sampling statement (~) may contain a non-linear transform of a parameter or local variable.
If it does, you need to include a target += statement with the log absolute determinant of the Jacobian of the transform.
Left-hand-side of sampling statement:
    y[i] ~ normal(...)

INFO:pystan:COMPILING THE C++ CODE FOR MODEL anon_model_05938347934a1e0dc0684abebfb5acbf NOW.
DIAGNOSTIC(S) FROM PARSER:
Info:
Left-hand side of sampling statement (~) may contain a non-linear transform of a parameter or local variable.
If it does, you need to include a target += statement with the log absolute determinant of the Jacobian of the transform.
Left-hand-side of sampling statement:
    y[i] ~ normal(...)

INFO:pystan:COMPILING THE C++ CODE FOR MODEL anon_model_8658615287c7659f9717aa982aed64fa NOW.
